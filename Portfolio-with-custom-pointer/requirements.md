## Packages
framer-motion | Complex page transitions, scroll animations, and hover effects
react-intersection-observer | Trigger animations when elements enter viewport

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
  mono: ["var(--font-mono)"],
}
API uses /api/contact for form submission
Dark theme is default
